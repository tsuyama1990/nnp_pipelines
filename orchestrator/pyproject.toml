[project]
name = "orchestrator"
version = "0.1.0"
description = "Orchestrator for Ace Active Carver"
requires-python = ">=3.10"
dependencies = [
    "ase",
    "numpy",
    "pyyaml",
    "pandas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
